function duplicates(numsArray)
{
    let repetidos = [];

    for (let i = 0; i < numsArray.length; i++) 
    {
        if (- && (numsArray[i].includes(i)))
        {

        }
        repetidos.push();
    }

    console.log(`Numeros: ${numsArray}`);
    return console.log(`Repetidos: ${repetidos}`);
}

const numerosRepetidosArray = [1, 2, 2, 3, 4, 4, 4, 5];
duplicates(numerosRepetidosArray);